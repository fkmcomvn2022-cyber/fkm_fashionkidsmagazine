# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Capacitor (Android) build output — keep the android/ project itself,
# ignore only generated build artifacts (xem [[fkm-studio-order-validation]]
# memory cho lý do tách app khỏi server — android/ chỉ là client)
android/app/build
android/build
android/.gradle
android/captures
android/local.properties
*.apk
*.aab

# Tauri (macOS) build output
src-tauri/target

# Real backend URL used to build native apps — không commit URL thật nếu
# chứa thông tin riêng, chỉ commit .env.production.example làm mẫu
.env.production

# Deploy secrets — không commit
deploy/render-env.local.txt
.firebaserc
