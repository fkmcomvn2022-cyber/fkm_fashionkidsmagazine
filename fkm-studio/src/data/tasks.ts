// Trước đây file này là 6 dòng "Việc nổi bật" MẪU cố định, không bao giờ đổi
// theo dữ liệu thật. Đã thay bằng `computeOperationTasks()` ở
// `src/lib/operationTasks.ts`, tính sống từ orders/customers/staff thật mỗi
// lần Home render (xem TaskBoard.tsx). Giữ file rỗng (không export gì) thay
// vì xoá hẳn — sandbox không cho xoá file qua shell lúc viết tính năng này.
export {};
